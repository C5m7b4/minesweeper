import React, { FC } from 'react';
import ReactDOM from 'react-dom';
import { GameWithHooks } from './modules/GameWithHooks/GameWithHooks';

const root = document.getElementById('root');

ReactDOM.render(<GameWithHooks />, root);
